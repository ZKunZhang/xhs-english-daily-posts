# 已发布说明

当前已发布目录拆成两层：

- 根目录下的日期文件夹
  是目前仍在使用的正式发布记录，优先保留完整发布稿和图片提示词。
- `00_早期文本记录`
  是更早期只有 `内容.txt` 的发布记录，保留做历史参考，不再和正式发布稿混在一起。

执行规则：

- 判断“是否发过”，优先看 `06_数据复盘/笔记列表明细表-0525.xlsx`。
- `04_已发布` 主要用于看历史稿子怎么写，不再单独承担去重唯一依据。
