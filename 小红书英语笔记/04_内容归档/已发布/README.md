# 已发布说明

当前已发布目录拆成两层：

- 根目录下的日期文件夹
  是目前仍在使用的正式发布记录，优先保留完整发布稿和图片提示词。
- `00_早期文本记录`
  是更早期只有 `内容.txt` 的发布记录，保留做历史参考，不再和正式发布稿混在一起。

执行规则：

- 判断“是否发过”，优先看 `05_数据复盘/` 中保留的最新笔记明细表。
- 如果最新数据表已超出统计窗口，再搜索本目录的历史稿和 `07_账号定位/笔记列表明细表_0525.md`。
