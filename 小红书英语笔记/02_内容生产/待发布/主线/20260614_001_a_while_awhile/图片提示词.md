# 2026-06-14｜a while / awhile 配图提示词

说明：封面由文字配图工具单独排版。AI 只生成 5 张内页配图。

## 统一原则

拍摄视角：

- 正面拍摄，纸面平整朝向镜头，无透视变形，文字水平。
- 画面里只有一页横线活页本或一张纸，四周留一点浅原木色桌面。
- 自然光从画面斜上方打过来，有轻微真实纸张光影。

纸张载体：

- 横线活页本内页，纸面略有轻微折痕、笔压痕和卷边。
- 背景保持浅原木桌面，桌面占画面四周 10%-15%。

文字风格：

- 手写体，但必须笔画粗、字号大、清楚可读，像黑色粗马克笔。
- 英文短语必须逐字准确，不要变形、乱码、假字。
- 每张图主要文字至少占页面 60% 视觉面积，留白清楚。
- 中文和英文都用黑色粗马克笔感，关键短语用浅黄或薄荷绿荧光笔轻扫。

冲突感：

- `a while` 和 `awhile` 必须形成明显视觉差异：一个无底，一个下方有薄荷绿荧光底。
- 红叉、绿勾用粗笔手绘感，尺寸控制在文字高度内。

禁止项：

- 不要 3D、渐变、商务风、PPT 排版、卡片式设计。
- 不要贴纸堆叠、复杂插画、虚拟人物、光晕滤镜。
- 不要让装饰元素遮挡任何一个英文字母。

## 图 1｜答案页（开门见山做对比）

构图：

页面用一条手绘竖线从上到下分成左右两半。左边写 `a while`，右边写 `awhile`。两边字号一样大，让“有没有空格”成为视觉焦点。

左半边文字：

`a while`

`一段时间 / 名词短语 / 两个词`

`for a while`

右半边文字：

`awhile`

`一会儿 / 副词 / 一个词`

`Please wait awhile.`

关键提示：

- `a while` 保持黑色无底，`awhile` 下方加薄荷绿荧光底。
- 中间竖线是普通粗笔画线，不做装饰栏。
- 整页只放这组对比，不加复杂解释。

## 图 2｜一句话记住

构图：

整页居中排版，顶部一个问题，中部两条判断规则。底部斜放一支木铅笔，不遮挡文字。

文字：

`前面有没有 for？`

`有 for → a while`

`没 for → 可以用 awhile`

关键提示：

- 两行规则字号一致。
- `a while` 和 `awhile` 用浅黄荧光笔轻轻盖一下。

## 图 3｜生活例句

构图：

整页竖向排列 4 条英文例句，每条前面一个粗黑小圆点，左上角有小标题。

文字：

`这样写才自然`

`I stayed for a while.`

`Let's talk for a while.`

`Please wait awhile.`

`Rest awhile before you leave.`

关键提示：

- 每条例句里的目标短语用浅黄荧光笔轻扫。
- 4 条例句间距一致，英文必须准确。

## 图 4｜避错页

构图：

页面分上下两半。上半部分是错误句，左侧红叉；下半部分是正确表达，左侧绿勾。中间一条手绘虚线分隔。

文字：

`这两句别再写错`

红叉：

`I stayed for awhile.`

`Let's talk for awhile.`

绿勾：

`I stayed for a while.`

`Let's talk for a while.`

底部：

`for 后面拆开写 a while`

关键提示：

- 红叉句里的 `for awhile` 用红色细线下划线。
- 绿勾句里的 `for a while` 用薄荷绿细线下划线。

## 图 5｜收藏关注

构图：

一张浅米黄色便利贴贴在活页本上，占页面中上部约 65%，右下角斜放木铅笔。

文字：

`先收藏，别再写错`

`有 for：a while`

`没 for：可以用 awhile`

`后面继续整理这种"看着会，其实容易错"的英语点。`

关键提示：

- 便利贴有轻微阴影和卷边。
- 字号要大，底部小字也必须清楚可读。
