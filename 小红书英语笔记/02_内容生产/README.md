# 内容生产说明

这个目录只放还会继续产出、修改或发布的内容，不再混入排期和历史归档。

## 目录

- `进行中/`：正在手工修改、尚未定稿的单条内容。
- `待发布/主线/`：已经能发布或只需小改的主线内容。
- `批量输出/`：批量生成的 Markdown 表格，不按日期拆成多个文件夹。

## 使用规则

- 做稿先放 `进行中/`。
- 定稿后移动到 `待发布/主线/`。
- 发布后移动或记录到 `04_内容归档/已发布/`。
- 批量生成内容统一放 `批量输出/`，再按排期拆成单条稿。
